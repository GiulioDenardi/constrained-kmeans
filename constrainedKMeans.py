class ConstrainedKMeans:
	def __init__(self):
		self.clusters = 0;
		self.dataset = [];

	def clusterize(clusters, dataset, mlCons, clCons):

	def __violateConstraints(point, cluster, mlCons, clCons):